---
interface Class {
  name: string;
  description: string;
  icon: string;
}

const classes: Class[] = [
  {
    name: "Academia de Danza",
    description:
      "Más que un lugar para aprender, somos una comunidad apasionada por la danza. Descubre tu potencial en un ambiente de confianza y creatividad.",
    icon: "💃",
  },
  {
    name: "Baby Ballet",
    description:
      "Inicia a los más pequeños en el mágico mundo del ballet. Clases diseñadas para fomentar coordinación, disciplina y amor por la danza desde temprana edad.",
    icon: "🩰",
  },
  {
    name: "Ballet Intermedio",
    description:
      "Eleva tu nivel con clases diseñadas para perfeccionar técnica, postura y expresión. Ideal para quienes buscan llevar su danza al próximo nivel.",
    icon: "🩰",
  },
  {
    name: "Ballet Avanzado",
    description:
      "Clases para bailarines comprometidos con la excelencia. Domina técnicas complejas y prepárate para brillar en escenarios profesionales.",
    icon: "🩰",
  },
  {
    name: "Ritmos Urbanos",
    description:
      "Siente la energía y la pasión de la danza urbana. Aprende movimientos frescos y expresivos al ritmo de los mejores beats.",
    icon: "🕺",
  },
  {
    name: "Jazz",
    description:
      "Libera tu energía con movimientos dinámicos y llenos de estilo. Nuestras clases de jazz combinan técnica, ritmo y diversión en cada sesión.",
    icon: "🎷",
  },
];

---
<section class="bg-gradient-to-b from-gray-50 to-white py-8 md:py-24 relative overflow-hidden">
  <!-- Efecto de textura -->
  <div class="absolute inset-0 bg-[radial-gradient(#e5e7eb_1px,transparent_1px)] bg-[size:30px_30px] opacity-20"></div>

  <div class="container mx-auto px-3 md:px-8 relative z-10">
    <!-- Título mejorado para mobile -->
    <h2 class="text-3xl md:text-6xl font-bold mb-8 md:mb-20 text-gray-900 text-center px-2">
      Descubre nuestras <span class="bg-gradient-to-r from-pink-600 to-red-600 bg-clip-text text-transparent">clases</span>
    </h2>

    <!-- Grid optimizado para 2 columnas en mobile -->
    <div class="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-3 md:gap-6">
      {classes.map((classItem, index) => (
          <article
              key={index}
              class="group relative rounded-xl md:rounded-2xl bg-gradient-to-r from-pink-500/20 via-red-500/20 to-yellow-500/20 p-[1px] md:p-[2px] shadow-md hover:shadow-lg transition-all duration-300 animate-fade-in-up"
              style={`animation-delay: ${index * 50}ms`}
          >
            <!-- Efecto de fondo vidriado -->
            <div class="absolute inset-0 rounded-xl md:rounded-2xl backdrop-blur-sm bg-white/50"></div>

            <div class="relative rounded-xl md:rounded-2xl bg-white/95 backdrop-blur-xs p-3 md:p-6 flex flex-col items-center h-full border border-white/20">
              <!-- Icono optimizado -->
              <div class="mb-3 md:mb-6 p-2 md:p-4 bg-gradient-to-br from-pink-100/40 to-red-100/20 rounded-xl md:rounded-2xl backdrop-blur-sm border border-white/20">
                <span class="text-4xl md:text-5xl bg-gradient-to-r from-pink-600 to-red-600 bg-clip-text text-transparent">
                  {classItem.icon}
                </span>
              </div>

              <!-- Contenido compacto -->
              <div class="flex-1 flex flex-col items-center space-y-2 md:space-y-4">
                <h3 class="text-base md:text-xl font-bold text-gray-900 mb-1 md:mb-4 text-center leading-tight">
                  {classItem.name}
                </h3>
                <p class="text-xs md:text-base text-gray-600 leading-snug md:leading-relaxed text-center backdrop-blur-xs bg-white/50 px-2 py-1 md:px-4 md:py-2 rounded-md md:rounded-lg">
                  {classItem.description}
                </p>
              </div>
            </div>
          </article>
      ))}
    </div>
  </div>

  <style>
    /* Optimizaciones mobile-first */
    @media (max-width: 640px) {
      .grid-cols-2 {
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }

      .group:hover {
        animation: none; /* Desactiva animaciones hover en mobile */
      }

      .backdrop-blur-xs {
        backdrop-filter: blur(1px);
        -webkit-backdrop-filter: blur(1px);
      }
    }
  </style>
</section>