---
const description: string =
  "En 2018, Juan De Dios, con más de 20 años de experiencia en la danza, fundó la Academia de Danza Di Ballu con el sueño de transformar vidas a través del arte. Su pasión y profesionalismo dieron vida a un espacio donde niños y jóvenes no solo aprenden a bailar, sino también a desarrollar disciplina, confianza y amor por la expresión artística. Con un equipo de docentes dedicados y programas diseñados para el crecimiento integral, Di Ballu se ha convertido en un hogar para quienes buscan superar sus límites y brillar. Aquí, cada paso cuenta una historia, y cada logro refleja el poder de la danza para inspirar y transformar.";
const imageUrl: string = "/images/historia/equipo.webp"; 
const galleryImages: string[] = [
  "/images/historia/galeria1.avif",
  "/images/historia/galeria2.avif",
  "/images/historia/galeria3.avif",
  "/images/historia/galeria4.avif",
  "/images/historia/galeria5.avif",
  "/images/historia/galeria6.avif",
  "/images/historia/galeria7.avif",
  "/images/historia/galeria8.avif",
]; 
---

<section class="bg-white py-12 md:py-24 relative overflow-hidden" id="history">
  <!-- Efecto de textura decorativa -->
  <div class="absolute inset-0 bg-[radial-gradient(#f3f4f6_1px,transparent_1px)] bg-[size:40px_40px] opacity-20"></div>

  <div class="container mx-auto px-4 md:px-8 relative z-10">
    <!-- Sección de descripción -->
    <div class="flex flex-col lg:flex-row items-center gap-8 md:gap-12 mb-16 md:mb-20">
      <div class="lg:w-1/2 space-y-6">
        <h2 class="text-4xl md:text-6xl font-bold text-gray-900">
          <span class="bg-gradient-to-r from-pink-600 to-red-600 bg-clip-text text-transparent">
            Nuestra Historia
          </span>
        </h2>
        <p class="text-lg md:text-xl text-gray-700 leading-relaxed backdrop-blur-xs bg-white/50 px-4 py-2 rounded-lg">
          {description}
        </p>
      </div>

      <div class="lg:w-1/2 flex justify-center">
        <div class="relative overflow-hidden rounded-2xl shadow-xl border border-gray-200/50 hover:border-pink-500/30 transition-all duration-300">
          <img
              src={imageUrl}
              alt="Equipo de la academia"
              class="w-full h-full object-cover transform transition-transform duration-500 hover:scale-105"
              loading="lazy"
          />
          <div class="absolute inset-0 bg-gradient-to-t from-black/20 via-transparent to-transparent"></div>
        </div>
      </div>
    </div>

    <!-- Galería mejorada -->
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4 md:gap-6">
      {galleryImages.map((image, index) => (
          <div class="group relative overflow-hidden rounded-xl shadow-lg hover:shadow-2xl transition-all duration-300 border border-gray-200/50 hover:border-pink-500/30">
            <div class="aspect-square overflow-hidden">
              <img
                  src={image}
                  alt={`Galería de la academia ${index + 1}`}
                  class="w-full h-full object-cover transform transition-transform duration-500 group-hover:scale-110"
                  loading="lazy"
              />
            </div>
            <div class="absolute inset-0 bg-gradient-to-t from-black/40 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300"></div>
          </div>
      ))}
    </div>
  </div>
</section>

<style>
  
  /* Texto descriptivo */
  .text-gray-700 {
    position: relative;
    padding: 1.5rem;
  }

  .text-gray-700::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 4px;
    height: 100%;
    background: linear-gradient(
      to bottom,
      rgba(219, 39, 119, 0.8),
      rgba(190, 24, 93, 0.6)
    );
    border-radius: 2px;
  }

</style>